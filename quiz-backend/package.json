{
  "name": "quiz-backend",
  "version": "1.0.0",
  "description": "",
  "main": "server.js",  // <-- Corregido para apuntar a tu archivo principal
  "scripts": {
    "preinstall": "rm -rf node_modules", // <--- AÑADIDO: Limpia antes de instalar
    "start": "node server.js",         // <--- AÑADIDO: Script común para iniciar
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "bcrypt": "^5.1.1",
    "cors": "^2.8.5",
    "express": "^4.19.2"  // <-- Nota: Express v5 aún no es estable, usando la última v4 estable
  }
}